{
  "name": "react-bundle-loaded",
  "version": "0.1.0",
  "description": "React component loaded with webpack bundle-loader",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tslint -c tslint.json \"src/**/*.ts\"",
    "test": "npm run lint",
    "build": "tsc",
    "prepublish": "npm run clean && npm test && npm run build"
  },
  "author": "Giff Song <giff.song@gmail.com>",
  "license": "MIT",
  "repository": "pawsong/react-bundle-loaded",
  "files": [
    "src",
    "lib"
  ],
  "peerDependencies": {
    "react": "^15.0.0"
  },
  "devDependencies": {
    "@types/react": "^15.0.24",
    "rimraf": "^2.6.1",
    "tslint": "^5.2.0",
    "typescript": "^2.3.2"
  }
}
